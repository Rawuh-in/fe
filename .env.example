# API Configuration
# Base URL for the backend API (Railway production or local development)
VITE_API_BASE_URL=http://localhost:8080

# Authentication Token (for development only - remove in production)
# Get this from logging in via Postman or the /login endpoint
VITE_AUTH_TOKEN=your_bearer_token_here

# Example production URLs:
# VITE_API_BASE_URL=https://be-service-production-2af6.up.railway.app
